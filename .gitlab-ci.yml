image: node:18

stages:
  - install
  - test

install_dependencies:
  stage: install
  script:
    - cd backend
    - npm install
  artifacts:
    paths:
      - backend/node_modules/

run_tests:
  stage: test
  script:
    - cd backend
    - npm test
  dependencies:
    - install_dependencies
